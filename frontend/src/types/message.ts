/**
 * Defines the structure of a message object used in the application.
 */
export interface Message {
    /** The unique identifier of the message. */
    id: number;

    /** The text content of the message. */
    text: string;
}